# Admin password — required. Keep private.
AUTH_SECRET=your-secret-here

# Read-only demo password — optional. Safe to share publicly.
DEMO_SECRET=your-demo-secret-here

# Override the SQLite database file path — optional. Defaults to db/seed.sqlite.
# DB_PATH=./db/seed.sqlite
